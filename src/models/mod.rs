/// the todo models, trait implementations and related
pub mod emails;
/// the notes models, trait implementations and related
pub mod notes;
/// the project models, trait implementations and related
pub mod projects;
/// the todo list models, trait implementations and related
pub mod todo;
/// the user request models, trait implementations and related
pub mod users;

/// contain shared model/entities
pub mod common;
